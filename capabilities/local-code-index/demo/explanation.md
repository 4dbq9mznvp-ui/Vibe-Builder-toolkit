This demo shows what a local code index should help with:

- answer an impact question before editing
- name likely affected files
- connect schema changes to tests and docs
- suggest verification commands

No local index or MCP server runs in this fixture. Future runners should require explicit setup and should never assume static analysis captures every dynamic framework behavior.
