This demo shows the value of a codebase knowledge graph:

- identify command routing, handlers, libraries, data files, and tests
- explain relationships before editing
- convert a broad question into a concrete edit plan
- separate observed structure from suggested work

No third-party graphing tool runs in this fixture. Future local runners should make graph freshness, ignored files, and generated-code handling explicit.
