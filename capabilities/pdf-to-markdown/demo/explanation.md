This demo shows the desired output style for a document-to-Markdown capability:

- preserve the document title as an H1
- turn metadata into clear Markdown
- preserve lists and questions
- convert simple tabular content into a Markdown table
- add review notes instead of pretending extraction quality is guaranteed

No third-party converter runs in this fixture. The goal is to make the capability understandable and reviewable before local runners are added.
