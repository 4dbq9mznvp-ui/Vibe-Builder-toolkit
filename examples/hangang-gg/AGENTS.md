# AGENTS.md

> Generated by agentsmd from `agentsmd.config.json` (single source of truth).
> Do not edit by hand — edit the config and run `agentsmd gen`.

## Project
**한강.gg** — 트레이더를 위한 매매 회고·전적 분석 SaaS.

## Tech stack
- Next.js
- TypeScript
- Supabase
- Tailwind CSS
- Vercel

Package manager: `pnpm`

## Commands
- `dev`: `pnpm dev`
- `test`: `pnpm test`
- `lint`: `pnpm lint`
- `typecheck`: `pnpm typecheck`
- `build`: `pnpm build`

## Conventions
- 서버/클라이언트 컴포넌트 역할을 분리한다.
- 데이터 접근은 서버 컴포넌트 또는 route handler에서 처리한다.
- 매매일지·전적 계산 로직은 순수 함수로 분리해 단위 테스트한다.

## Do NOT
- 시크릿이나 .env 파일을 커밋하지 않는다.
- 사용자의 매매 데이터를 RLS 없이 노출하지 않는다.
- any 타입을 남용하지 않는다.

## Security
- Supabase RLS로 사용자별 데이터를 격리한다.
- auth.uid() 기반 정책으로 본인 데이터에만 접근하게 한다.
- API route에 rate limit과 입력 검증을 둔다.

## Definition of done (review before finishing a task)
- 타입체크와 린트를 통과한다.
- RLS 정책을 바꾸면 접근 제어 테스트를 추가한다.
- 회고 통계 계산에 단위 테스트를 추가한다.

## Current priorities
- 인증·세션 안정화
- 매매일지 CRUD
- 전적 통계 대시보드
